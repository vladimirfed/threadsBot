import path from 'node:path';

export const PROMPT = `
Context: Ты — мой цифровой двойник.

Requirements:
- Язык: русский. Строго человеческий, рациональный, логичный, естественный стиль; имитируй мой тон, словарный запас и типичную длину предложений.
- Длина треда: минимум 10, максимум 460 символов.
- Формат: один чистый текст треда — НЕТ вводных комментариев, НЕТ метаданных, НЕТ цитат.
- Контент: слегка кликбейтный заголовок/хук + 1–2 коротких «байта» для комментариев (вопрос/провокация).
- Стиль: короткие абзацы или одна-две фразы; поддерживать читабельность и «человечность».
- Ограничения: не использовать эмодзи в начале, не вставлять ссылки, не добавлять инструкции исполнителю.

Task: Напиши тред для Threads на тему 'Topic' указанную ниже.
'Style reference' указан ниже.
`.trim();

export const AI_CONFIG = {
  model: 'gemini-2.5-flash',
  maxRetries: 2,
  threadCharLimit: 500,
} as const;

export const TOPICS = [
  'один лайфхак по продуктивности, который реально работает',
  'что бы я сказал себе 5 лет назад',
  'преимущества индексного инвестирования',
  'пузырь на рынке недвижимости в Праге',
  'почему женщинам слоднее заработать деньги (в про феменистичном контексте)',
  'какие льготы должны быть у женщин (в про феменистичном контексте)',
  'усталость от думскроллинга и как с ней бороться',
  'новости ИИ (краткий обзор с комментарием)',
  'новости финансового рынка мира/Америки (краткий обзор с комментарием)',
] as const;

export const CONFIG = {
  dataDir: path.resolve('data'),
  get textStyle(): string {
    return path.join(this.dataDir, 'textStyle.txt');
  },
  get postCache(): string {
    return path.join(this.dataDir, 'post-cache.json');
  },
} as const;

export const THREADS_MAX_LENGTH = 500;
